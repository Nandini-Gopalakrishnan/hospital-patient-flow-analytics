This folder includes Python scripts used for data partitioning and preprocessing.
Due to the large size of the datasets (approximately 100,000 records per table), the data was divided into smaller, structured subsets to ensure efficient loading and improved performance during import into MySQL Workbench.
